# Environment Variables for ctrlf
# Copy this file to .env and fill in your actual API keys

# Ollama Base URL
# Optional: Base URL for Ollama server (default: http://localhost:11434)
# Only needed if Ollama is running on a different host/port
# Example: http://localhost:11434 or http://remote-server:11434
OLLAMA_BASE_URL=http://localhost:11434

# OpenAI API Key
# Required when using the OpenAI provider (e.g., gpt-4o)
# Get your API key from: https://platform.openai.com/api-keys
# Format: sk-... (starts with "sk-")
OPENAI_API_KEY=sk-your-openai-api-key-here

# Google API Key (for Gemini)
# Required when using the Gemini provider (e.g., gemini-2.5-flash)
# Get your API key from: https://makersuite.google.com/app/apikey
# Format: Long alphanumeric string (typically 39+ characters)
GOOGLE_API_KEY=your-google-api-key-here

# Note: Ollama provider (default) does not require an API key
# Ollama runs locally and doesn't need authentication
